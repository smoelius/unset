name: CI

on:
  pull_request:
  push:
    branches:
      - master
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            ~/.rustup/toolchains/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Test
        run: cargo test

      - name: Clippy
        run: cargo clippy --all-targets -- --deny=warnings

      - name: Fixture with unset
        working-directory: fixture
        env:
          VARIABLE_TO_LEAVE_UNCHANGED: unchanged
        run: |
          cargo test --config "target.'cfg(all())'.runner = '../target/debug/unset'"
